# yaml-language-server: $schema=https://raw.githubusercontent.com/dbt-labs/dbt-jsonschema/main/schemas/latest/dbt_project-latest.json
name: italy_dashboard
version: "1.0.0"
profile: italy_dashboard

# Run from the repo root:  just transform
# (all relative paths below resolve against the repo root cwd)

model-paths: ["models"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  italy_dashboard:
    staging:
      +materialized: view
    marts:
      # Marts land as Parquet files the dashboard picks up directly
      # (italy_dashboard/queries.py builds views from data/marts/*.parquet).
      +materialized: external

# A year needs this many observed days to count as complete. MUST equal
# italy_dashboard.queries.MIN_DAYS_FOR_A_FULL_YEAR (a unit test pins it): the
# marts gate precipitation normals and anomalies on it, the query layer gates
# every plotted annual series on it, and the two must agree on what a year is.
vars:
  min_days_for_a_full_year: 360

seed-paths: ["seeds"]

seeds:
  italy_dashboard:
    +quote_columns: false
    province_capitals:
      +column_types:
        province_code: varchar
        province_name: varchar
        capital_city: varchar
        region_code: varchar
        region_name: varchar
        lat: double
        lon: double
        elevation_m: double
        cell_elevation_m: double
    violent_crime_codes:
      +column_types:
        crime_code: varchar
        crime_name: varchar
